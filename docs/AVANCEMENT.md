# Set et Brique, avancement

État du développement de la plateforme, module par module de la spécification (`specification-fonctionnelle.md`), avec le journal des étapes. La doc technique est dans `FONCTIONNEMENT.md`. Mis à jour à chaque étape.

Dernière mise à jour : 23 septembre 2026.

## 1. Où on en est

| Module (spécification) | État | Ce qui est en place | Ce qui manque |
| --- | --- | --- | --- |
| 1. Catalogue et stock | **Fait** | Fiche set complète (numéros multiples, marque, notices papier/numérique, figurines, dimensions, temps de montage, commentaire public, poids interne, caution, battement par set), 10 photos max, multi-exemplaires, statuts calculés Disponible / Location / Battement / Réparation / Retiré, catalogue et fiche publics, les 28 sets de la grille de la cliente importés | Photos des sets, vérification par la cliente des gammes déduites et des textes |
| 2. Planning des locations | **Fait en partie** | 5 lieux de remise, durée libre à 2 €/jour, jours calendaires, battement entre clients différents et aucun battement pour le même client qui enchaîne, périodes fermées, blocage par set via « En réparation » | Planning propre à chaque lieu (une remise à la fois) non contrôlé : validation manuelle |
| 3. Comptes clients et suivi | **Fait** | Compte Clerk, fiche client à la première demande avec nom, prénom, téléphone, adresse obligatoires, compte bloqué par les gérants, espace `/compte` avec ses réservations, gestion du compte `/compte/profil` (Clerk + onglets Coordonnées et Historique), lieu de remise préféré, suivi récupéré / à rendre / retard sur chaque location | Rien de bloquant ; la purge des données au bout de 3 ans reste à écrire (voir §2) |
| 4. Paiement Stripe | **Pas commencé** | Colonnes Stripe prévues en base, statut `pending_payment` | Tout le paiement, la caution, les délais de blocage (1 h, 15 min). Bloqué par deux questions ouvertes, voir §3 |
| 5. Tunnel de réservation | **Fait** | Set → dates → lieu → coordonnées → CG → demande en attente ; validation manuelle par les gérants : accepter, refuser, proposer d'autres dates ; réponse du client ; annulation. Le calendrier de dates est consultable **sans compte** ; la connexion n'est demandée qu'à la validation des dates choisies, qui sont reprises telles quelles au retour | Paiement (module 4), bon cadeau (module 6) |
| 6. Bons cadeaux | **Commencé** | Table `gift_vouchers` (migrations 0010, 0013) : montant, origine achat/admin, statut valide/utilisé/annulé, **numéro de lot** (`LOT-0001`…, un par génération même à l'unité), note interne, expiration. Écran admin `/admin/bons-cadeaux` : génération en fenêtre (bouton « Créer des bons cadeaux »), liste en tableau groupée par lot (repliable), filtres, marquer utilisé, annuler, impression d'un bon (page dédiée + styles `@media print`). Côté public : page `/bons-cadeaux` (montants, conditions, commande par e-mail pré-rempli), lien dans le header, bandeau sur l'accueil | Achat en ligne par le client (paiement, module 4), utilisation comme moyen de paiement dans le tunnel de réservation, email d'envoi du bon |
| 7. Contrat et CG | **Fait en partie** | Case à cocher obligatoire, horodatée en base | Contrat PDF généré, texte légal définitif |
| 8. Notes | **Pas commencé** | | Tout. La facturation est remplacée par l'émission de notes (révision de la spécification du 18 septembre 2026) : génération au paiement, mentions allégées, numéro `aaaa-mm-##`, note complémentaire pour le retard, export par lot en zip |
| 9. État des lieux et dommages | **Commencé** | Statut « En réparation » et note interne par exemplaire, état des lieux en commentaire libre au retour, retard calculé et affiché | Séquence de retard J-1 / J / J+1 / J+2, barème, forfaits, et le solde de la restitution par deux boutons (Restitution conforme / Établir une note complémentaire) |
| 10. Notifications | **Pas commencé** | Historique `booking_events` sur lequel se brancher | Fournisseur d'email à choisir (aucune clé dans le projet), templates modifiables |
| 11. Back-office et reporting | **Fait en partie** | Admin : tableau de bord (blocs cliquables, CA du jour/semaine/mois), sets (tableau avec recherche et filtres), forfaits, lieux, fermetures, réservations, bons cadeaux, statistiques (CA 12 mois, sets les plus loués, taux d'occupation 30 jours, bons cadeaux). Rôles admin et superadmin | Contenus du site, maintenance, réglages, export des ventes sur une période (champs début et fin, bouton année civile ; CA total + une ligne par client) et export des notes par lot en zip |
| 12. Déploiement et formation | **En cours** | Déploiement Vercel automatique, base Neon partagée | Instance Clerk de production, domaine final, formation |

## 2. Hypothèses prises sans règle explicite

À valider avec la cliente, à corriger si elle tranche autrement.

- La remise ne peut pas être demandée pour le jour même : la première date proposée est demain.
- Une demande acceptée passe en « acceptée, paiement à venir » (`pending_payment`) et s'arrête là tant que le module 4 n'est pas construit.
- L'exemplaire est attribué dès la demande, pas à la confirmation, pour bloquer les dates. Les gérants pourront le changer.
- Le client peut annuler lui-même tant que sa demande n'est pas acceptée ; ensuite il doit contacter les gérants (la politique d'annulation du module 7 s'applique après paiement).
- « Blocage par set » (module 2) = passer l'exemplaire « En réparation » ; pas de blocage par dates propre à un set.
- **Les gérants ne modifient pas les dates d'une demande.** Le client a choisi ses jours ; s'ils ne conviennent pas, la demande est refusée avec un motif et il refait une demande. Les gérants ajustent seulement le lieu et l'heure de remise. Décision du 16 septembre 2026, qui retire la proposition d'autres dates mise en place le matin même.
- **Heure de remise demandée dans le tunnel.** La spécification (module 2) prévoyait « une date, pas d'heure » en V1, l'heure se négociant ensuite. Décision du 16 septembre 2026 : le client indique une heure souhaitée (par quart d'heure) que les gérants confirment ou modifient en proposant d'autres dates. Ça évite un aller-retour par téléphone dans le cas simple.
- **Conservation des données clients : 3 ans après la dernière activité** (dernière location terminée ou dernière connexion), ou dès la suppression du compte par le client. Décision du 16 septembre 2026. Les notes, contrats et pièces comptables vivent dans le logiciel comptable, avec leurs propres durées légales (6 ans fiscal, 10 ans comptable) ; la plateforme n'a donc pas à les porter — elle les émet et permet de les exporter, elle n'en est pas l'archive. Passé le délai : suppression du compte Clerk et des coordonnées, réservations conservées anonymisées (statistiques). Reste à faire : la purge elle-même (script ou tâche planifiée), l'export du contrat et de l'acceptation des CG vers l'archive comptable au moment de l'émission de la note, et la mention dans la politique de confidentialité.


## 3. Questions ouvertes avec la cliente

Deux points en attente dans la spécification bloquent le module 4 :

1. Le paiement est-il pris **avant ou après** la validation manuelle ? Et en cas de refus d'une demande déjà payée, remboursement automatique ?
2. Si le loyer est payé par **TPE** sur place, comment se pose la pré-autorisation de la caution ?

Tous les points ouverts par la révision du 18 septembre 2026 (notes et exports) ont été tranchés le jour même : la spécification des notes ne porte plus aucune question ouverte.

**À ne pas oublier à la construction du module** (demandé par avance le 23 septembre 2026, avant même que le paiement existe, pour ne pas l'oublier) : le tunnel de réservation annonce déjà, dans l'encart Location/Caution, qu'un bon cadeau pourra être renseigné « au moment du paiement » (voir `FONCTIONNEMENT.md`, section Tunnel de réservation) — cette phrase n'est vraie pour l'instant que sur le papier, il faudra vraiment câbler la saisie d'un bon cadeau à cette étape.

**Bons cadeaux, commande par e-mail en attendant Stripe** (24 septembre 2026, message préparé pour Marion par Alexis) : Marion accepte-t-elle de prendre les commandes de bons par e-mail depuis le site ? Si oui, quel règlement pour ces commandes (virement, espèces, TPE à la remise) ? Faut-il mettre un montant en avant (« le plus offert ») ou laisser les trois au même niveau ? La page `/bons-cadeaux` est en ligne avec la commande par e-mail ; elle sera ajustée selon ses réponses.

Autres points à poser quand l'occasion se présente :

- Délai minimal entre la demande et la remise (aujourd'hui : dès le lendemain).
- Faut-il une durée minimale de location au-delà d'un jour ? (réglage `min_rental_days`, à 1)
- Domaine final du site : lequel, et chez quel registrar ? Nécessaire pour l'instance Clerk de production (voir §4).
- Grille des sets : la gamme (Star Wars, Ideas, Technic…) a été déduite du numéro de boîte, à vérifier. Trois sets sans gamme : Coupe du Monde (43020), La mine de l'Ouest (Pantasy 85025), Échecs pirate (40158). La description du Faucon Millenium cite le 75105 alors que le numéro saisi est 75257. Le set « Test : voiture de course » reste publié.

## 4. Prochaines étapes, dans l'ordre proposé

1. Emails transactionnels : choisir un fournisseur (Resend est le plus simple avec Vercel), ajouter la clé dans Vercel, envoyer aux transitions demande reçue / acceptée / refusée, puis set remis / set rendu. Les gérants doivent pouvoir modifier les textes (module 10).
2. Paiement Stripe et caution (module 4), une fois les deux questions tranchées.
3. Contenus du site et réglages dans l'admin (avis, presse, textes, battement par défaut).
4. Séquence de retard, état des lieux et barème (module 9), puis notes (8), contrat PDF (7), bons cadeaux (6), reporting et exports (11).

### Passage de Clerk en production (module 12)

Clerk reste le service de comptes en production : plan gratuit suffisant, aucun changement de code. La vérification de l'e-mail à l'inscription et les mails de compte (mot de passe oublié, codes) sont envoyés par Clerk, pas par un service à nous. Étapes, dans l'ordre, une fois le domaine connu :

1. Rattacher le domaine au projet Vercel.
2. Dans le Dashboard Clerk, renommer l'application « Set et Brique » et créer l'instance de production sur ce domaine.
3. Ajouter les cinq CNAME fournis par Clerk (deux pour l'authentification, trois pour les mails). Si le domaine est chez Vercel, `vercel dns add` suffit ; sinon les saisir chez le registrar. Attendre la validation par Clerk.
4. Créer des identifiants Google OAuth dans la console Google Cloud et les renseigner dans Clerk : la connexion Google de l'instance de développement ne fonctionne pas en production.
5. Remplacer `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` et `CLERK_SECRET_KEY` dans Vercel (Production), redéployer.
6. Les comptes de l'instance de développement ne sont pas repris : Marion et Gaëtan recréent leur compte, puis `pnpm role <email> admin` (et `superadmin` pour Agon-Gate).

## 5. Journal

### 24 septembre 2026

- **Connexion et inscription, plus de liseré rouge autour des champs en saisie** (`app/layout.tsx`, `clerkAppearance`). Deux contours rouges se cumulaient au focus d'un champ Clerk (e-mail, mot de passe) : l'anneau de Clerk, tiré de `colorPrimary`, et le contour `:focus-visible` global du site (`globals.css`). L'anneau passe en gris-bleu discret (`colorRing`) et les champs reçoivent la classe `focus-outline-none` (`elements.formFieldInput`) : le focus reste visible, sans le rouge réservé aux actions négatives. Vaut pour tous les composants Clerk du site (connexion, inscription, profil).
- **Fiche d'un set, ouverture en haut de page** (`components/scroll-to-top.tsx`, nouveau ; `app/catalogue/[slug]/page.tsx`). En cliquant sur un set depuis le bas du catalogue, on arrivait au milieu de la fiche : Next.js garde la position de défilement tant que la nouvelle page reste visible dans la fenêtre (même cause que celle déjà contournée dans le menu de l'admin, `components/admin/nav.tsx`). Un petit composant client remet la fenêtre en haut à l'arrivée, sans animation (`behavior: "instant"`) pour ne pas voir la page remonter avec le `scroll-behavior: smooth` global.
- **Tunnel de réservation, bouton « Choisir mes dates » en rouge tant qu'aucune date n'est choisie** (`app/catalogue/[slug]/reserver/booking-form.tsx`) : `btn-brick` au lieu de `btn-paper`, pour que la première action à faire saute aux yeux. Une fois la période choisie, le bouton devient « Modifier les dates » et reprend le style blanc (`btn-paper`). Demande d'Alexis (24 septembre 2026). Le rouge est ici celui du bouton d'action principal (comme « Réserver ce set »), pas une alerte.
- **Catalogue, filtres dans une colonne à gauche et nouveaux critères** (`app/catalogue/page.tsx`, `app/catalogue/catalogue-browser.tsx` nouveau, `lib/format.ts` `parseBuildHours()`, `app/globals.css` `.range-dual`). Demande d'Alexis : filtres par gamme à gauche, plus disponible dès aujourd'hui, âge conseillé, nombre de pièces et temps de montage (ces deux derniers au curseur). Choix laissés à Claude (« ce qui est le mieux, lisible et pratique pour les clients ») :
  - Âge : un menu « Âge du constructeur » qui garde les sets conseillés jusqu'à cet âge (un parent cherche pour son enfant), plutôt que des tranches exclusives.
  - Curseurs à deux poignées (minimum et maximum) plutôt qu'un simple maximum, pour pouvoir aussi chercher les gros sets. Poignées de 28 px pour le doigt, plage sélectionnée en jaune (sélection, comme le calendrier), contour de focus sur la poignée.
  - Filtrage dans le navigateur, sans rechargement ; l'adresse suit les filtres (lien partageable, `?gamme=` gardé tel quel). Compteurs des gammes recalculés selon les autres filtres, gammes vides grisées.
  - Mobile : bouton « Filtres » avec le nombre de filtres actifs, qui déplie le panneau au-dessus de la liste.
  - Retouche après relecture : sets sur **3 colonnes** à côté des filtres (une première version en 2 colonnes jugée trop large par carte). Pour tenir, la page du catalogue passe de `max-w-6xl` à `max-w-7xl` (même largeur que le header et le footer), la colonne de filtres de 16 à 15rem, et les cartes sont resserrées : `p-4`, titre `text-lg`, gamme/numéro en `text-xs`, pièces/âge/montage en `text-sm`, prix `text-xl`, caution `text-xs`. 3 colonnes à partir de `xl` (1280 px) ; entre `lg` et `xl`, 2 colonnes, les cartes n'ayant sinon plus qu'environ 215 px de large.
  - Deuxième retouche : plus de liseré rouge autour du menu « Âge du constructeur » au focus (`focus-outline-none`, bordure bleue au clavier), et gamme sélectionnée en bleu (`bg-sea`) au lieu du rouge.
  - Troisième retouche : la gamme passe d'une liste de boutons à un menu déroulant, comme l'âge (« Toutes les gammes (29) », « Harry Potter (3) »…). Style partagé entre les deux menus (`selectClass`).
  - Quatrième retouche : la gamme remonte avant l'âge — c'est le premier critère des clients (Alexis) ; « Disponible dès aujourd'hui » reste tout en haut. Ordre final : Disponible dès aujourd'hui, Gamme, Âge du constructeur, Nombre de pièces, Temps de montage.
- **Revue UX du site, corrections** (demande d'Alexis, tri point par point) :
  - **Bug : un set archivé revenait « Publié »** (`app/admin/sets/set-form.tsx`). Le menu Statut de la fiche était un `select` contrôlé ; après l'enregistrement, React réinitialise le formulaire de l'action et un `select` contrôlé revient alors visuellement à son option initiale. L'archivage était bien enregistré, mais le menu réaffichait « Publié » (on croyait que ça n'avait pas marché), et un second enregistrement renvoyait « Publié » en base. Passé en non contrôlé (`defaultValue`) comme les autres champs. Vérifié : la validation accepte bien l'archivage du set de test ; seul ce menu avait ce montage (les `input` contrôlés ne sont pas touchés, React synchronise leur attribut `value`). À archiver ensuite depuis l'admin : « Test : voiture de course ».
  - Statut client « En battement » → **« Bientôt de retour »** (`setAvailabilityLabels`, vu uniquement côté client ; l'admin garde « battement »).
  - **Écrans de chargement** (`loading.tsx`) sur le catalogue, la fiche d'un set et la réservation : silhouettes grises de la page à venir (`components/skeleton.tsx`, pulsation coupée si l'utilisateur limite les animations, texte pour lecteur d'écran). Vérifié en capture via une page d'aperçu temporaire, supprimée ensuite.
  - Calendrier de réservation **fermé à l'arrivée** : sur mobile il couvrait la page ; le bouton rouge « Choisir mes dates » l'ouvre.
  - « Concept » → **« Comment ça marche »** partout (lien du footer, surtitre de la section de l'accueil), comme le header.
  - **Temps de montage toujours en heures** côté client (`formatBuildTime()`, `lib/format.ts`) : « 120 minutes » → « 2 h », « 1,5 h » → « 1 h 30 » ; une fourchette en heures reste telle quelle. Catalogue et fiche.
  - Laissés de côté sur décision d'Alexis : commande des bons cadeaux par formulaire plutôt que `mailto:` (pas avant la démo du 25 septembre), prix affiché une seule fois dans le catalogue (le tarif peut évoluer par set). Textes de l'accueil qui contredisaient le tarif (2 €/jour sans dégressivité depuis le 13 septembre), corrigés après validation d'Alexis : accroche « …loués à la semaine autour de Lorient. Sans les acheter, sans les stocker. » → « …loués pour la durée de votre choix autour de Lorient. » ; étape 1 « …Plus vous louez longtemps, moins la journée coûte cher. » → « Parcourez le catalogue et choisissez vos dates. La durée est libre : vous ne payez que les jours où vous gardez le set. » Aucun prix écrit en dur, le tarif pouvant évoluer.
- **Onglet flottant « Réserver un set » retiré** (`components/floating-reserve-button.tsx` supprimé, `app/layout.tsx`, `components/site-header.tsx`). Proposé par Alexis (« peu d'intérêt »), recommandation validée : doublon du lien « Catalogue » du header sur ordinateur, masquait du contenu sur tablette, icône seule peu compréhensible sur mobile, et détournait de l'objectif sur la page des bons cadeaux. Seul vrai usage, le mobile où le menu est replié : remplacé par un bouton « Catalogue » dans le header, à gauche du burger, sous `lg`. Header resserré sous 380 px et nom du site masqué sous 340 px pour tenir sur une ligne. Vérifié en captures à 320, 360, 375, 390 et 768 px (trait jaune sur `/catalogue`). Le burger a gagné un `aria-label` (« Ouvrir le menu » / « Fermer le menu »), il n'en avait pas.
- **Vérification responsive** (mobile 375 px, tablette 768 px, petit portable 1024 px ; captures Edge headless, méthode dans `DESIGN-SYSTEM.md`). Pages vues : accueil, catalogue, fiche d'un set, réservation, bons cadeaux, connexion. Corrigé : cartes du bas de `/bons-cadeaux` qui débordaient à droite sur mobile (bouton de l'adresse e-mail trop large) ; ticket « Offrez un bon cadeau » de l'accueil au texte écrasé sur tablette (en colonne jusqu'à `lg`), bouton du ticket sur deux lignes sur mobile, montants insécables (« 30 € ») ; boutons « Commander ce bon » sur deux lignes et désalignés sur tablette ; menu « Toutes les gammes (29) » tronqué dans la colonne de filtres (compteur retiré de cette option, le total est déjà dans l'en-tête). Ajouté au passage : lien « Bons cadeaux » dans le footer. Rien à signaler sur le catalogue (1, 2 puis colonne de filtres + 2 colonnes), la fiche, la réservation (bouton rouge « Choisir mes dates » visible) et le header à 1024 px (tient sur une ligne, trait jaune sous la page courante). Non vérifiable ainsi : le formulaire Clerk de `/connexion`, le panneau de filtres ouvert sur mobile et le glisser des curseurs (il faut cliquer).
- **Header, page courante indiquée** (`components/site-header.tsx`) : le lien de la page où l'on se trouve (Catalogue, Bons cadeaux, Qui sommes-nous, Connexion) est souligné d'un trait jaune, sous-pages comprises, dans la barre desktop et le menu mobile (`usePathname()`, `aria-current="page"`). Demande d'Alexis. Jaune plutôt que rouge (réservé aux actions) ; trait plutôt que pastille pour ne pas élargir une barre déjà pleine. Vérifié : rendu serveur de `/catalogue` et `/bons-cadeaux` (bon lien marqué), rien de marqué sur l'accueil.
- **Bons cadeaux côté public** (`app/bons-cadeaux/page.tsx` nouveau, `app/page.tsx`, `lib/site.ts`, `components/site-header.tsx`, `app/admin/forfaits/actions.ts`). Décision avec Alexis après proposition : une page dédiée plutôt qu'une simple zone de l'accueil (l'acheteur d'un cadeau n'est pas le client qui loue, il y a des conditions à expliquer, l'achat en ligne aura besoin de sa page, et le lien se partage sur les réseaux à Noël ou à la fête des mères), plus un lien dans le header et un bandeau jaune (`studs`) sur l'accueil, après « Nos gammes ». Commande par e-mail pré-rempli en attendant le paiement Stripe (questions posées à Marion, §3). Header : avec ce lien en plus, la barre desktop ne tenait plus sur une ligne entre `lg` et `xl` ; le lien « Vinted » y est masqué (`wideOnly` dans `nav`), il reste visible au-delà de `xl` et dans le menu mobile. Vérifié : types, lint, rendu serveur de `/bons-cadeaux` (5, 10 et 15 jours, liens e-mail) et de l'accueil (bandeau entre les gammes et les avis, lien dans le header et le menu mobile). Pas vérifié à l'écran : la largeur réelle du header entre 1024 et 1280 px. Retouche : la page s'ouvre toujours en haut (`ScrollToTop`, comme la fiche d'un set) — depuis le bandeau du bas de l'accueil, on y arrivait au milieu. Bandeau de l'accueil redessiné en ticket (talon, ligne de découpe pointillée, encoches), sans blanc ni rouge : icône et bouton en bleu nuit sur le jaune (retour d'Alexis : « le blanc gêne, le jaune donne un côté ticket gagnant »). Page resserrée en hauteur (en-tête `py-8 md:py-10`, section des montants `pt-8 md:pt-10`, cartes `py-6`) pour que la phrase « Sur la base de 2,00 € par jour… » soit visible dès l'arrivée sur un écran d'ordinateur portable ; titre « Choisissez un montant » renommé « Choisissez votre bon cadeau ». Icône de l'en-tête alignée sur celle du ticket de l'accueil (sans pastille blanche, inclinée). Idée d'un bandeau flottant permanent sur tout le site discutée et déconseillée (voir réponse du 24 septembre) ; non construite. Seconde retouche : les cartes « Comment ça marche » reprennent exactement celles de « Simple comme un jeu d'enfant » de l'accueil (bordure haute épaisse rouge, bleu nuit, jaune ; icône 56 px ; mêmes marges), au lieu de simples `brick-card` sans couleur. Troisième retouche, « trop de rouge, trop agressif » (Alexis) : le rouge ne reste que sur la barre de la première étape (reprise de l'accueil). Titre : « de construction » d'abord surligné en jaune, puis, à la demande d'Alexis, seul le mot « construction » en rouge, comme « Construisez. » dans le titre de l'accueil ; icône cadeau en bleu nuit sur pastille à barre jaune ; boutons « Commander ce bon » en jaune (`btn-sun`, texte bleu nuit), assortis à la barre jaune des cartes de montant.
- **Accueil, section « Nos gammes »** (`app/page.tsx`, `app/admin/sets/actions.ts`). Demande d'Alexis, sur le modèle d'un concurrent qui affiche ses thèmes en blocs d'images : une tuile par gamme, placée avant les avis Google. Adapté au design du site plutôt que copié : cartes arrondies `brick-card` (pas de blocs carrés collés), photo du premier set de la gamme, nom en blanc sur un dégradé bleu nuit en bas de l'image (lisible sur toutes les photos, là où le concurrent pose un cartouche blanc translucide), nombre de sets, léger zoom de la photo au survol. Grille 2 colonnes sur mobile, 3 sur tablette, 4 sur ordinateur, puis un bouton « Voir le catalogue », rouge, identique à celui du haut de la page (d'abord « Voir tout le catalogue » en blanc, aligné sur le haut de page à la demande d'Alexis). Chaque tuile ouvre le catalogue déjà filtré sur la gamme. Pas de tuile « Autres » pour les sets sans gamme (un bloc « Autres » n'a pas d'attrait sur un accueil). L'accueil lit maintenant la base mais reste statique (régénéré à chaque modification d'un set, voir `FONCTIONNEMENT.md` §8). Vérifié : types, lint, rendu de l'accueil en local (15 gammes, liens corrects, section avant les avis) ; deux gammes n'ont encore aucune photo et s'affichent sur fond bleu nuit à pois.
  - Temps de montage ajouté sur les cartes, puisqu'on peut maintenant filtrer dessus.
  - Vérifié : types, lint, conversion des temps de montage sur des exemples, rendu serveur de `/catalogue` avec et sans filtres dans l'adresse (résultats contrôlés à la main). Pas vérifié : le glisser des curseurs et l'affichage réel, faute de navigateur dans l'environnement.

### 23 septembre 2026

- **Bouton de création d'un bon cadeau renommé** (`app/admin/bons-cadeaux/forms.tsx`) : « Ajouter un bon cadeau » → « Créer des bons cadeaux », plus juste puisqu'un seul clic permet de générer un lot de plusieurs bons d'un coup, pas un seul.
- **Création d'un forfait passée en fenêtre** (`app/admin/forfaits/forms.tsx`, `page.tsx`) : `RatePlanCreateForm`, formulaire fixe affiché sous la liste des forfaits existants, remplacé par `RatePlanCreateDialog`, même patron que `GiftVoucherCreateDialog` (bouton « Créer un forfait » à droite du titre, modale qui se ferme d'elle-même une fois le forfait créé). La section « Nouveau forfait » en bas de page a disparu.
- **Même changement pour les lieux de remise** (`app/admin/lieux/forms.tsx`, `page.tsx`) : `PickupPointCreateForm` → `PickupPointCreateDialog` (bouton « Créer un lieu »), en réutilisant tel quel `PickupPointFields` (nom, adresse, consignes, créneaux, actif) déjà partagé avec le formulaire de modification. Modale un peu plus large (`max-w-2xl`) que celles des bons cadeaux et forfaits pour laisser la place à ses deux colonnes de champs.
- **Même changement pour les périodes fermées** (`app/admin/fermetures/forms.tsx`, `page.tsx`) : `BlackoutCreateForm` → `BlackoutCreateDialog` (bouton « Créer une période de fermeture »), en réutilisant `BlackoutFields` (dates du/au, motif) déjà partagé avec le formulaire de modification.
- **Réservations, passage des cinq listes de cartes à un tableau unique** (`app/admin/reservations/bookings-table.tsx`, nouveau ; `page.tsx` simplifié pour ne faire que la requête et le mapping en lignes). Filtres repris du modèle sets/bons cadeaux : pastilles de raccourci (les cinq groupes de statut d'avant, avec compteur, cliquables) plus une recherche libre. Colonnes Période et Lieu masquées sur mobile (`hidden sm:table-cell`) pour ne pas forcer un défilement horizontal sur un tableau qui sera d'abord consulté au téléphone. Décision avec l'utilisateur (Alexis, 23 septembre 2026) après une proposition à deux options (tableau seul avec actions en icônes, vs. cartes « à traiter » gardées à part) : ligne entière cliquable ouvrant une pop-up de détail plutôt que des icônes minuscules dans la ligne, plus confortable au doigt.
  - Pop-up (`booking-dialog.tsx`) : résumé (référence, set, client, période, lieu, heure, retard éventuel) commun à tous les statuts. Pour une demande à traiter, les trois boutons Accepter/Modifier/Refuser d'avant, repris tels quels (`list-actions.tsx`, inchangé sauf l'ajout d'un `onSuccess` optionnel pour fermer la pop-up une fois la décision prise). Pour les autres statuts, un bouton « Voir la fiche complète » vers `/admin/reservations/[id]`, qui reste seule responsable des actions Set remis/Set rendu, de la note interne, du blocage client et de l'historique — pas de duplication de cette logique dans la pop-up.
  - Tri : par groupe de statut (même ordre qu'avant), puis par date de retour pour les locations en cours (`picked_up`, pour faire remonter les retours les plus proches), sinon par date de début.
- **Deux retouches après relecture (23 septembre 2026)** : les pastilles de statut, d'abord dans un bloc séparé au-dessus du bloc de recherche, réunies dans le même `brick-card` que la recherche (« Statut » à côté de « Recherche »), sur le modèle exact du bloc de filtres des sets — cohérence demandée avec l'existant. Les lignes « à traiter » (`pending_review`) se distinguent maintenant du reste du tableau par un fond et un liseré gauche jaunes (`border-sun-deep bg-sun/20`, badge d'état assorti `bg-sun`) plutôt qu'une ligne visuellement identique aux autres : à volume, une demande urgente ne doit pas se noyer dans la liste.
- **Encore deux retouches (23 septembre 2026)** : le filtre Statut, d'abord en pastilles cliquables de libellés différents, remplacé par un menu déroulant unique (`<select>`, options « Tous » puis chaque groupe avec son compte) — plus conforme au patron des filtres des sets et des bons cadeaux. Dans la pop-up de détail, le bouton « Voir la fiche complète » (statuts autres que « à traiter ») aligné à droite en bas de la pop-up plutôt qu'en bloc à gauche sous le texte.
- **Filtre Statut, sélection multiple (23 septembre 2026)** : le `<select>` simple (un seul groupe à la fois) remplacé par un menu déroulant maison à cases à cocher — bouton affichant « Tous », le nom du groupe si un seul est coché, ou « N statuts sélectionnés » sinon, qui ouvre un panneau (`div` positionné en absolu, fermeture au clic extérieur via un `pointerdown` sur `document`) listant les cinq groupes avec leur compte et une case à cocher chacun, plus « Tout désélectionner ». Choix du menu maison plutôt qu'un `<select multiple>` natif, peu lisible et peu maniable au doigt sur mobile.
- **Confirmation du refus, en fenêtre plutôt qu'en place (23 septembre 2026)** (`app/admin/reservations/list-actions.tsx`). Le bouton Refuser utilisait `ConfirmButton` dans son mode par défaut (le bouton se transforme sur place en « Oui, refuser »/« Annuler ») — jugé pas assez visible une fois noyé dans la pop-up de détail à trois boutons. Passé en `asDialog` (fenêtre centrée « Refuser ? Cette action est définitive. »), le même mode déjà utilisé pour supprimer un set ou un exemplaire — aucun nouveau composant, seuls `asDialog` et `state={refuseState}` ajoutés à l'appel existant.
- **Ajout de photos sur la fiche d'un set, deux retouches (23 septembre 2026)** (`app/admin/sets/[id]/sections.tsx`) : le bouton « Ajouter les photos » passé de rouge (`btn-brick`) à vert (`btn-leaf`), rouge n'étant pas approprié pour une action positive d'ajout. La liste des photos sélectionnées, d'abord une phrase avec les noms de fichiers mis bout à bout séparés par des virgules, passée en encart (fond bleuté, comme les autres blocs de message) avec un nom de fichier par ligne — illisible en une seule phrase dès que les noms sont longs ou nombreux.
- **Sous-titres de section du formulaire d'un set, plus en rouge** (`app/admin/sets/set-form.tsx`, `Group`) : « IDENTITÉ », « CONTENU »… en `text-brick-deep` par défaut de composant, alors que le rouge est réservé aux actions destructrices/négatives (voir `DESIGN-SYSTEM.md`) — ce ne sont que des intitulés de regroupement, sans charge négative. Un premier essai en `text-ink-deep` (bleu marine, comme les titres neutres de l'admin) jugé pas assez distinct du reste du texte ; passés en `text-sun-deep` (jaune), la couleur de mise en avant déjà utilisée ailleurs sur le site pour ce qui doit ressortir sans être une alerte.
- **Bouton Enregistrer d'un lieu de remise, aligné à droite** (`app/admin/lieux/forms.tsx`, `PickupPointRow`) : sans `flex justify-end`, il atterrissait dans la première colonne de la grille du formulaire, juste sous la case « Proposé aux clients » — pas à droite comme partout ailleurs dans l'admin (fiche d'un set, exemplaire, forfait…). Corrigé sur le même patron que ces formulaires.
- **Tunnel de réservation, mention des bons cadeaux** (`app/catalogue/[slug]/reserver/booking-form.tsx`) : une phrase ajoutée sous la caution, dans l'encart Location — « Si vous disposez d'un bon cadeau, vous pourrez le renseigner au moment du paiement. » Message posé par avance : l'utilisation d'un bon cadeau comme moyen de paiement dans le tunnel n'est pas encore construite (module 6), seul le loyer réglé sur place (TPE à la remise) existe aujourd'hui.

### 22 septembre 2026

- **Fusion `bons-cadeaux` confirmée fusionnée dans `main` la veille par Madus** (voir entrée du 21). La branche `bons-cadeaux`, entièrement contenue dans `main`, supprimée (locale et distante, déjà absente côté distant). Nouvelle branche de travail `srogath`.
- **Avertissements ESLint préexistants nettoyés** (`components/reviews-carousel.tsx`) : deux `eslint-disable-next-line react-hooks/exhaustive-deps` devenus inutiles, retirés.
- **Bons cadeaux, liste passée en tableau** (`app/admin/bons-cadeaux/vouchers-list.tsx`), sur le modèle de `SetsTable` : mêmes styles de filtres, mêmes badges. Formulaire de création déplacé dans une fenêtre (bouton « Ajouter un bon cadeau », vert, en haut à droite du titre — comme « Ajouter un set »), qui se ferme automatiquement une fois le bon créé.
- **Numéro de lot** (migration 0013). Chaque génération (unitaire comprise) reçoit un numéro `LOT-0001`, `LOT-0002`… tiré d'une séquence Postgres (`gift_voucher_batch_seq`), pour regrouper l'affichage. Les bons déjà en base ont été rattachés rétroactivement à un lot reconstitué (mêmes minute de création, étiquette et montant) : hypothèse non vérifiable sur les données réelles (lecture directe de la table en prod refusée par les garde-fous de l'environnement), signalée avant d'appliquer la migration. Tableau réorganisé en une ligne par lot, dépliable (chevron) pour voir chaque code avec ses actions.
- **Impression d'un bon cadeau** (`app/admin/bons-cadeaux/[id]/imprimer/`). Page dédiée (logo, code, montant, validité, mentions), bouton « Imprimer » déclenchant `window.print()` — pas de moteur PDF serveur pour ce besoin (décision : rester sur l'impression navigateur, le futur moteur PDF partagé du contrat/des notes, modules 7/8, se décidera séparément). Règle `@media print` générique ajoutée à `app/globals.css` (`.print-area` / `.no-print`), réutilisable telle quelle plus tard.
- **Cohérence des actions par ligne**. « Annuler ce bon » déplacé sous le code, à gauche de la ligne (composant `GiftVoucherCancelButton`), séparé des actions positives à droite (Imprimer, Marquer utilisé) — même logique que « Supprimer cet exemplaire » sous « Enregistrer » ailleurs dans l'admin, pas un simple alignement à l'intérieur d'une même colonne (premier essai insuffisant : la colonne Actions se rétrécissait au contenu, l'alignement gauche/droite à l'intérieur ne se voyait pas). Bouton « Marquer utilisé » repris du style du bouton « Modifier » du tableau des sets (fond `bg-ink-deep` plein). Badge « Utilisé » passé de gris à vert (`bg-green-50 text-leaf-deep`). Lignes individuelles d'un lot zébrées (fond alterné) pour mieux les distinguer visuellement, ligne de lot gardant un fond bleuté permanent (pas seulement au survol).
- **Tunnel de réservation accessible sans compte jusqu'au choix des dates** (`app/catalogue/[slug]/reserver/`). Avant, la page redirigeait immédiatement vers `/connexion` pour un visiteur non connecté. Le calendrier de disponibilité et le choix des dates sont maintenant visibles sans compte ; la connexion n'est demandée qu'au clic sur « Valider ces dates » dans le calendrier, avec les dates choisies reprises dans l'URL de retour (`?start=...&days=...`, validées et pré-remplies au retour de connexion) pour ne pas les faire ressaisir. Les sections Coordonnées et CGU/Envoi restent masquées tant que le visiteur n'est pas connecté (nouvelle prop `authenticated` de `BookingForm`) ; le contrôle serveur de `submitBookingRequest` (déjà présent) reste la protection de dernier recours.
- **Actions des bons cadeaux passées en icônes** (imprimante, coche), avec info-bulle native (`title`, nouvelle prop de `SubmitButton`) plutôt qu'un libellé texte — le bouton « Imprimer les bons valides du lot » ajouté juste avant débordait de sa colonne et forçait tout le tableau à défiler horizontalement dans une fenêtre de taille normale. Ligne de lot dépliée mise en évidence par un fond bleu pâle (`bg-sea/8`, réduit une fois jugé trop soutenu), qui disparaît une fois repliée.
- **Bon cadeau imprimé, refondu en carte de visite** (85 × 55 mm, paysage, `print-voucher-card.tsx`) avec un **QR code** (librairie `qrcode`, génération locale, aucun appel à un service externe) qui mène au catalogue public. Il pointe vers `site.url` (nouveau champ dans `lib/site.ts`, alias Vercel stable `https://set-et-brique.vercel.app` en attendant le domaine final — question encore ouverte, voir §3) : un seul endroit à changer le moment venu. Impression d'un lot : les cartes s'enchaînent côte à côte sur la page (`break-inside-avoid`) plutôt qu'une par page A4, pour ne pas gâcher de papier vu leur petite taille.
- **Bug d'impression, page 2 quasi vide**. La règle générique masquant tout sauf `.print-area` (ajoutée un peu plus tôt dans la session) utilisait `visibility: hidden`, qui cache un élément sans lui retirer sa place dans la mise en page : le reste de la page admin (menu, en-tête), invisible mais toujours haut de plusieurs centaines de pixels, poussait une deuxième page quasi vide à l'impression. Corrigé avec `display: none` sur tout ce qui n'est ni `.print-area`, ni son contenu, ni un de ses ancêtres (sélecteur groupé avec `:has()` et `:not()`, pas trois règles séparées : une exception oubliée repasse par la règle générale et disparaît aussi — bogué une première fois en oubliant le contenu de `.print-area` lui-même, carte visible mais vide).
- **Fiche d'un set loué ou en battement, la réservation ne se bloque plus**. Avant, la page affichait « Être prévenu de son retour » (un simple lien `mailto:`) dès que le set n'était pas disponible *aujourd'hui*, sans possibilité de réserver pour plus tard alors que le calendrier du tunnel gère déjà très bien les dates futures au cas par cas (`findFreeCopy` par jour). Un set loué ou en battement avec une date de retour connue affiche maintenant « Vous pouvez le réserver à partir du [date] » et garde le bouton « Réserver ce set », qui mène au même tunnel — le calendrier y grise les jours pris et laisse cliquer les jours libres, sans changement nécessaire côté tunnel. L'ancien lien de contact reste, sous un intitulé plus précis (« Être recontacté par e-mail à sa remise en stock »), en option secondaire sous les boutons plutôt qu'en CTA unique — un vrai suivi par e-mail (capture d'adresse, envoi automatique au retour) reste à construire avec le reste des notifications (module 10, aucun fournisseur d'e-mail choisi à ce jour). Réparation et retiré, sans date de retour connue, gardent l'ancien comportement (lien de contact seul, pas de bouton Réserver).
- **Bug corrigé, en deux temps : la date « à partir du » annoncée par la fiche ne correspondait pas toujours au calendrier juste en dessous** (`lib/availability-core.ts`, `computeSetAvailability`, utilisée aussi par la pastille de statut du catalogue et de la fiche — `AvailabilityBadge`, « de retour le … »).
  1. La date se déduisait uniquement de la fin de la location en cours plus le battement (`endDate + turnaroundDays + 1`), sans tenir compte d'une réservation déjà enregistrée juste après sur le même exemplaire — alors que le calendrier, lui, vérifie vraiment chaque jour un par un avec `findFreeCopy`. Remplacé par une recherche jour par jour avec cette même fonction.
  2. Persistait encore après ce premier correctif : `loadAvailability` (`lib/availability.ts`) n'allait chercher en base que les réservations qui occupent réellement un exemplaire aujourd'hui (`OCCUPYING_STATUSES` : acceptée, en location, rendue tardivement), pas les **demandes encore en attente** (`pending_review`, `date_proposed`) — alors que le calendrier de la fiche (`loadDayAvailability`), lui, les inclut déjà (`RESERVING_STATUSES`), justement pour qu'une date déjà demandée ne soit pas proposée à quelqu'un d'autre. Le nouveau calcul jour par jour avait donc raison sur le papier mais recevait des données incomplètes. `loadAvailability` va maintenant chercher les mêmes statuts que le calendrier ; `computeSetAvailability` ne garde que les statuts réellement occupants pour le statut affiché aujourd'hui (une demande en attente ne doit pas faire passer le set en « loué »), mais utilise l'ensemble pour chercher la prochaine date libre, dates proposées par les gérants comprises.
- **Les deux calendriers de disponibilité s'ouvrent maintenant directement sur le premier mois avec un jour libre**, plutôt que sur le mois en cours par défaut — gênant quand ce mois est déjà complet, il fallait cliquer plusieurs fois sur « mois suivant » avant de voir quoi que ce soit de disponible. Calendrier de la fiche set (`components/catalogue/availability-calendar.tsx`) : cherché dans `days`, déjà chargé. Calendrier du tunnel (`app/catalogue/[slug]/reserver/booking-calendar.tsx`) : recherché jour par jour avec `dayStatus()` au premier rendu (borné à 12 mois) ; une période déjà choisie avant un aller-retour par la connexion (`initialStartDate`, voir l'entrée sur le tunnel accessible sans compte) prime sur cette recherche, le calendrier rouvre alors sur son mois à elle.
- **Pastille de statut d'un set loué, refondue** (`components/catalogue/availability-badge.tsx`) : le rappel « En location » passe en petit texte discret, et c'est la date de retour qui prend le dessus — plutôt que l'inverse (gros badge marine, date en tout petit à côté) qui ne donnait pas envie de cliquer. Catalogue et fiche d'un set utilisent cette même pastille ; la phrase redondante qui la répétait juste au-dessus du bouton « Réserver ce set » sur la fiche (ajoutée plus tôt dans la session) a été retirée, la pastille suffit désormais. Reprise deux fois après relecture : une phrase en rouge marque essayée d'abord, jugée trop proche des messages de blocage ; puis une seconde pastille au même style que « Disponible » (fond jaune) avec « Disponible à partir du [date] », d'abord recollée à « En location » sur la même ligne et peu lisible, écartée en dessous avec un vrai espace (`mt-2 block`, pas `inline-block`).

### 21 septembre 2026

- **Fusion de la branche `bons-cadeaux` dans `main`** (13 commits : refonte de l'espace de gestion, tableau des sets, page Statistiques, calendrier du tunnel de réservation, carrousel de photos, créneaux multiples par lieu, design système). Huit fichiers en conflit, tranchés ainsi :
  - **Sécurité, la règle de `main` l'emporte** : `requireRole("admin")` reste en tête de chaque page de l'admin (module 12, protection sortie du proxy), y compris sur la nouvelle page `/admin/statistiques` qui en était dépourvue. `/api/customer-name` ne lit que la fiche du compte connecté, rien à changer.
  - **Disponibilités, une seule implémentation** : la branche avait extrait les calculs purs dans `lib/availability-core.ts` pendant que `main` y ajoutait le calendrier mensuel de la fiche set. Les fonctions dupliquées (`findFreeCopy`, `bookedRange`, `isInBlackout`, `isBookable`) sont celles du noyau ; `computeDayAvailability` et le type `DayAvailability` y ont été déplacés (calcul pur, donc utilisable côté navigateur), `loadDayAvailability` restant dans `lib/availability.ts` (accès base). `monthStart`, doublon de `monthStartIso` (`lib/dates.ts`) apporté par la branche, supprimé.
  - **Fiche d'un set** : la nouvelle mise en page en deux colonnes de la branche est conservée, et le calendrier des disponibilités de `main` y prend place dans la colonne de droite, sous le bouton de réservation. L'ancienne deuxième section (description et « En bref » répétés) disparaît.
  - **Bons cadeaux** : filtres instantanés côté client (`vouchers-list.tsx`) de la branche, à la place du filtrage SQL par URL de `main`.
  - **Documentation** : journal du 20 septembre placé avant celui du 18, état du module 11 fusionné (ce qui est fait vient de la branche, ce qui manque garde les exports détaillés décidés le 18).
- Vérifications : `tsc --noEmit`, `pnpm lint` (deux avertissements préexistants dans `components/reviews-carousel.tsx`) et `pnpm build` passent. Migrations 0011 et 0012 enchaînées proprement après 0010, journal Drizzle cohérent.

### 20 septembre 2026

- **Photos d'un set**. Sélection multiple dans le champ fichier, avec une description commune facultative. Le premier essai envoyait tout le lot en une seule requête (`addSetImages`) : 10 photos dépassaient `serverActions.bodySizeLimit` (10 Mo, prévu pour une photo) et Next rejetait tout avec « Unexpected end of form » (corps tronqué). Corrigé en repassant l'envoi à une photo par appel (`addSetImage`), invoqué en boucle depuis le client plutôt que via un `<form action>` : une requête par photo, jamais bridée par la taille du lot, quel qu'il soit. Fenêtre centrée à l'écran pendant l'envoi (photo en cours, avancement, barre de progression). Retrait d'une photo passé en un clic (plus de confirmation en deux temps type « Retirer » → « Supprimer » : jugée inutile et peu lisible pour une action facilement réversible). Curseur et couleur au survol sur ce bouton et sur `ConfirmButton` (`components/admin/form.tsx`, utilisé par les suppressions d'exemplaire, de set, etc.) : un `<button>` HTML n'a pas de curseur main par défaut, contrairement à un lien ; même traitement sur les flèches de réorganisation. Description d'une photo déplacée hors du formulaire d'import (un texte commun aux 10 photos d'un lot n'avait pas de sens) vers un petit champ sous chaque vignette, enregistré au blur (`updateSetImageAlt`). Flèches de réorganisation reliées différemment (`formAction` lié par photo plutôt qu'un formulaire partagé avec bouton nommé) ; la logique de tri en base était déjà correcte (vérifié à la main), mais ce câblage est plus robuste. Ajustements après relecture : flèches remises à l'horizontale (← → au lieu de ↑ ↓, plus lisible dans une grille de photos), état « en cours » visible pendant le déplacement (boutons grisés, « … » — `useFormStatus` dans un composant dédié, `MoveButtons`), formulaire d'ajout repassé sur deux lignes pour que le texte d'aide du champ Description ne décale plus le bouton (bouton en bas à droite, comme les autres formulaires de l'espace de gestion, et même style plein que le bouton Ajouter des Exemplaires juste en dessous). Ajout du glisser-déposer pour réordonner les photos directement (nouvelle action `reorderSetImages`, qui écrit l'ordre complet), les flèches restant le chemin clavier. Bouton d'ajout d'un nouvel exemplaire renommé « Ajouter l'exemplaire » (au lieu de « Ajouter » seul), pour matcher les autres boutons de création de l'admin (« Ajouter le lieu », etc.).
- **Ajustements demandés sur le profil**. Nom et prénom affichés à droite de l'avatar dans le header (au lieu de la gauche). Bouton Enregistrer de l'onglet Coordonnées passé à droite (comme les fiches de l'espace de gestion). Bouton « Mettre à jour le profil » de l'onglet Compte (page native Clerk) renommé « Enregistrer » par surcharge de la localisation ; sa position reste celle de Clerk, cet onglet n'étant pas un de nos composants.
- **Onglet Coordonnées du profil** : astérisque sur les six champs obligatoires (prénom, nom, téléphone, adresse, code postal, ville), avec légende. Ville proposée à partir du code postal (API `geo.api.gouv.fr`, sans clé, appelée depuis le navigateur ; `app/compte/profil/postal-city-fields.tsx`) : remplissage automatique si le champ est vide et une seule commune correspond, sinon suggestions dans la liste du champ.
- **Nom du client dans le header**. Une fois prénom et nom renseignés dans l'onglet Coordonnées, ils s'affichent à côté de l'avatar (desktop). Lu via une route `/api/customer-name` appelée côté client (même logique que le rôle admin : éviter de rendre tout le site dynamique), rafraîchie à la connexion et après chaque enregistrement des coordonnées (événement `customer-profile-updated`).
- **Confirmation de suppression en fenêtre pour l'exemplaire et le set**. Le bouton qui se transforme sur place (Confirmer / Annuler) restait en place pour le reste (lieux, forfaits, bons cadeaux…), mais jugé peu visible pour ces deux suppressions plus lourdes de conséquence. `ConfirmButton` (`components/admin/form.tsx`) prend une prop `asDialog` qui affiche une fenêtre centrée à la place, sans dupliquer le composant. Bug du premier essai : fermer la fenêtre au clic sur « Confirmer » via `onClick={() => setArmed(false)}` sur ce bouton `type="submit"` lui-même démontait le bouton avant que le navigateur n'ait déclenché l'envoi du formulaire — la suppression ne partait jamais, silencieusement. Corrigé en passant l'état de l'action (`state`, `useActionState` du parent) à `ConfirmButton` : la fenêtre se referme d'elle-même une fois l'action terminée sans erreur (changement de `state` détecté pendant le rendu, pas de fermeture manuelle au clic), et affiche l'erreur dans la fenêtre le cas échéant.
- **Exemplaires d'un set passés en onglets, en haut de la fiche**. Avant, une liste en bas de page ; maintenant des onglets juste sous le nom du set (un par exemplaire, plus « + Nouvel exemplaire »), avant même la fiche et les photos — on choisit d'abord l'exemplaire sur lequel on travaille. Le panneau sous les onglets montre soit l'édition de l'exemplaire choisi, soit le formulaire de création ; bascule automatique sur le nouvel exemplaire après création, ou sur le premier restant si celui affiché est supprimé. Onglet et panneau teintés pour tout exemplaire autre que le premier ; d'abord une seule teinte bleu clair pour tous (`bg-sky/40`), jugée trop discrète et pas assez distinctive entre exemplaires — passé à une couleur différente par exemplaire (jaune, vert, bleu, en boucle). Ajustée une nouvelle fois après relecture : d'abord trop marquée, opacités réduites ; et la couleur du prochain exemplaire est désormais visible dès l'onglet « + Nouvel exemplaire », avant même sa création.
- **Envoi de photos, ne plus s'arrêter à la première erreur**. Une photo refusée (trop lourde, mauvais format) stoppait tout le reste du lot. Le lot continue maintenant jusqu'au bout ; le message final liste les échecs, en regroupant une erreur répétée à l'identique (ex. limite de 10 photos atteinte, qui se reproduirait pour chaque photo suivante) en une seule ligne avec un compteur plutôt que de la répéter.
- **Panneau d'un exemplaire, resserré et protégé sur le dernier**. Espace entre Enregistrer et Supprimer cet exemplaire réduit (`mt-6 pt-4` → `mt-2 pt-2`) : le bloc prenait trop de place. Sur le dernier exemplaire d'un set, le bouton de suppression disparaît (remplacé par une explication : ça reviendrait à vider le set, autant l'archiver ou le supprimer entier) ; `deleteCopy` refuse aussi côté serveur dans ce cas, pas seulement côté écran.
- **Date d'entrée en stock sur un exemplaire** (migration 0011, `set_copies.stock_entry_date`, nullable). Saisie à la main par les gérants, pas déduite de la date de création de la fiche. Pour ne pas agrandir le bloc, le formulaire passe de quatre champs sur deux colonnes à cinq sur trois (`sm:grid-cols-3`) : même nombre de lignes qu'avant. Pré-rempli à aujourd'hui pour un nouvel exemplaire, vide par défaut sur les exemplaires existants ; l'exemplaire créé automatiquement à la création d'un set l'est aussi (oublié au premier essai, corrigé après relecture).
- **Zone d'ajout de photos, rendue visiblement cliquable**. Le champ fichier nu ne se voyait pas comme une zone d'action. Remplacé par une zone en pointillés avec icône et texte « Cliquez pour choisir des photos » (`<label>` habillant l'`<input type="file">`, masqué mais toujours accessible en `sr-only`), qui affiche ensuite le nombre et le nom des photos choisies avant l'envoi.
- **Limite de 10 photos annoncée avant l'échec**. À 10/10, la zone d'ajout de photos est remplacée par un message expliquant qu'il faut en retirer une avant d'en ajouter — avant, il fallait tenter l'envoi pour le découvrir (l'erreur serveur existait déjà, juste pas anticipée à l'écran).
- **Nom du client dans le header, repensé**. Le prénom affiché en texte brut à côté de l'avatar ne plaisait pas visuellement. Regroupé avec l'avatar dans une seule pastille (bordure, fond blanc, coins arrondis) plutôt que deux éléments qui flottent côte à côte ; prénom seul (pas le nom de famille), pour rester compact.
- **Pastille du header, cliquable dans son ensemble**. Avant, seul l'avatar (`UserButton` de Clerk) réagissait au clic, en ouvrant un menu ; le prénom à côté ne faisait rien. Remplacé par un lien direct vers `/compte/profil` sur toute la pastille (avatar en initiale + prénom). `UserButton` étant aussi la seule façon de se déconnecter du site, ajouté un bouton dédié à côté (icône, `SignOutButton` de Clerk) ; ajouté aussi au menu mobile, qui n'avait jamais eu de déconnexion directe.
- **En-tête du catalogue resserré**. Le bandeau (titre, description, compteur de sets) prenait trop de place à l'écran avant même la liste. Titre et compteur remis sur une même ligne, marges et paddings resserrés (`app/catalogue/page.tsx`). Phrase de présentation retirée juste après (« la caution est bloquée sur votre carte... sauf casse ou perte ») : jugée à même de faire peur aux clients dès l'arrivée sur le site.
- **Fiche d'un set, ordre inversé**. Le bloc « En bref » (pièces, figurines, dimensions…) remonte sous le nom du set, avant le prix et le bouton Réserver, qui passent juste en dessous — auparavant « En bref » était plus bas, dans une deuxième section à côté de la description. `app/catalogue/[slug]/page.tsx`.
- **Photos d'un set, en carrousel plutôt qu'en vignettes** (`app/catalogue/[slug]/image-carousel.tsx`, nouveau composant client). La grille de miniatures sous la photo principale a disparu ; les photos se parcourent depuis la photo elle-même, avec des flèches gauche/droite superposées et un compteur (« 3 / 10 »). L'espace ainsi libéré est repris par la description (« Le set ») et le commentaire public (« Bon à savoir »), qui vivaient avant dans une section séparée plus bas — la page tient maintenant dans une seule section.
- **Calendrier de sélection des dates dans le tunnel de réservation**. Avant, deux champs (« Date de remise », « Nombre de jours ») sans vue sur la disponibilité. Remplacés par une fenêtre calendrier, ouverte automatiquement à l'arrivée sur `/catalogue/<slug>/reserver` : mois par mois, jours libres cliquables, jours indisponibles grisés, sélection de la période en deux clics (remise, puis retour), durée et prix estimé affichés en direct. Résumé de la période affiché sur deux lignes nettes (dates courtes sans jour de semaine, puis durée et prix) plutôt que la durée entre parenthèses collée aux dates, qui se chevauchait dans la fenêtre étroite. Extrait la logique pure de disponibilité (`findFreeCopy`, `isInBlackout`, `computeSetAvailability`…) de `lib/availability.ts` (qui importe `lib/db`, donc injouable côté navigateur) vers un nouveau module `lib/availability-core.ts`, sans aucune dépendance serveur, réutilisable tel quel dans le calendrier client — `lib/availability.ts` réexporte tout depuis ce module, aucun des fichiers qui l'importaient déjà n'a eu besoin de changer.
- **Couleurs et lisibilité du calendrier de réservation, ajustées après relecture**. La période sélectionnée surlignée en rouge (`brick`) pouvait faire croire à une erreur ; passée en jaune (`sun`), le rouge restant réservé aux vrais messages de blocage (durée trop courte, période indisponible). Le prix (fenêtre et résumé du formulaire) passé du rouge au vert (`leaf-deep`). Ajouté le jour de la semaine et l'année sur les dates affichées. Repris une nouvelle fois après relecture : la phrase « Du ... au ... » entièrement en gras était difficile à lire d'un coup d'œil. Nouveau composant `DateReadable` (`date-readable.tsx`) : jour et mois en gras, jour de semaine et année en texte normal.
- **Menu de la console renommé « Espace de gestion »** (`components/admin/nav.tsx`, cohérent avec la pastille du header). Retour en haut de la fenêtre à chaque changement de section : par défaut, Next.js garde la position de défilement tant que la page précédente reste visible, ce qui était toujours le cas ici (menu sticky).
- **Page Statistiques** (`/admin/statistiques`). CA des 12 derniers mois en barres, cinq sets les plus loués, taux d'occupation glissant sur 30 jours, valeur des bons cadeaux (émis / utilisés / en circulation). Nouvel utilitaire `monthStartIso` dans `lib/dates.ts`. Repli « Bonjour » sur `/compte` quand ni la fiche client ni Clerk n'ont de prénom (affichait « Bonjour » suivi d'un espace vide).
- **Raccourci vers l'espace de gestion**. Pastille jaune « Espace de gestion » dans le header (desktop et mobile) pour les comptes admin et superadmin, seul accès avant en dehors de `/compte`. Le bouton « Réserver un set » est masqué pour ces mêmes comptes.
- **Refonte du menu de la console** (`components/admin/nav.tsx`). Titre « Console de gestion » en pastille jaune (identique à celle du header), une icône par section, hiérarchie de graisse (actif en gras et fond bleu marine, inactifs plus légers), séparation visuelle de la section Maintenance (superadmin). Nouvel onglet Statistiques entre Bons cadeaux et Contenus du site, sans page pour l'instant.
- **Tableau de bord enrichi** (`app/admin/page.tsx`). Trois rangées de blocs cliquables : catalogue (Sets, Clients, Bons cadeaux valides), activité de location (Sets en location, Réservations à traiter, Sets à remettre, vers `/admin/reservations`), et chiffre d'affaires (jour / semaine / mois, sur les réservations confirmées ou au-delà, à la date de début de location). Nouveaux utilitaires de date dans `lib/dates.ts` (début de semaine et de mois).
- **Page Sets passée en tableau** (`components/admin/sets-table.tsx`). Recherche (nom, numéro de set) et filtres (statut, thème) instantanés côté client, sans rechargement. Le tableau est à largeurs fixes pour ne jamais déborder sur desktop, avec défilement horizontal en secours sur mobile.
- **Fiche d'un set, lisibilité**. Titres de bloc (Identité, Contenu…) en pastille rouge sur fond teinté au lieu de la même couleur que le reste du texte ; badge de statut identique à celui de la liste ; alignement des champs corrigé quand un champ a une aide plus longue que son voisin (`components/admin/form.tsx`, le champ pousse maintenant en bas de sa cellule) ; bouton Enregistrer aligné à droite ; message « Modification enregistrée » (au lieu de « Set enregistré ») pour une fiche existante. Le champ Statut est passé en composant contrôlé : il affichait parfois l'ancienne valeur après un enregistrement.
- **Liseré de focus**. Le liseré rouge global (`:focus-visible` dans `globals.css`, hors des « layers » Tailwind donc prioritaire sur les utilitaires `focus-visible:*`) est neutralisé sur les champs de filtre des sets via une classe dédiée (`.focus-outline-none`), réutilisable ailleurs si besoin.
- **Accès rapide à « Mes locations »**. La pastille de compte du header (desktop) mène maintenant directement à `/compte` (« Mes locations ») plutôt qu'à `/compte/profil` : ouvre en fait un **menu déroulant** (« Mes locations », « Gérer mon compte », puis « Se déconnecter » sous un filet, le bouton dédié de déconnexion étant retiré du header). Panneau ouvert vers la droite (`left-0`), fermeture au clic extérieur, à Échap ou après un choix. Menu mobile mis à jour à l'identique (deux liens séparés au lieu d'un seul « Mon compte »).
- **Connexion admin, arrivée directe dans l'espace de gestion**. `/compte` redirige désormais un compte admin/superadmin vers `/admin` (la connexion pointant déjà vers `/compte`) ; le bouton « Espace de gestion » qui y figurait devient inutile, retiré.
- **Bouton « Réserver un set » sorti du header**. Devenu un onglet flottant accroché au bord droit de l'écran (`components/floating-reserve-button.tsx`), masqué sur `/catalogue/**` (même destination) et `/admin/**`. Repositionné à 1/3 de la hauteur d'écran (pas tout en bas, plus visible) puis redessiné en onglet à texte vertical après un premier essai en bulle flottante bas-droite jugé peu esthétique ; réduit à une icône seule sous `sm` pour ne pas empiéter sur le contenu en mobile.
- **Vignettes de la fiche d'un set**. Colonne verticale de vignettes cliquables à gauche de la photo principale (au lieu d'une simple grille sous la photo), limitée à 4 vignettes visibles avec défilement au-delà pour ne pas allonger la page.
- **Couleurs de survol de l'espace de gestion, reprises plusieurs fois**. Menu de gestion et tuiles du tableau de bord d'abord passés au jaune (`sun`) façon bandeau « demande à traiter », jugé au final pas dans le ton voulu ; repassés au bleu (`bg-sea/15`), avec la même intensité partout après un premier réglage incohérent entre le menu (trop faible) et les tuiles. Chiffres du tableau de bord (sets, clients, réservations…) repassés du rouge au bleu foncé (`text-ink-deep`), comme le CA : le rouge reste réservé aux actions, pas à l'affichage d'un chiffre.
- **Bouton « Définir comme forfait par défaut »**. Transformé en vrai bouton (`btn-paper`) au lieu d'un lien souligné, et remonté avant la ligne de suppression du forfait. Bouton « Enregistrer » d'un forfait aligné sur le style des autres fiches (rouge plein, au lieu du style « paper » utilisé jusque-là).
- **Sous-titres de l'espace de gestion, uniformisés**. `text-sm` ajouté aux paragraphes sous le titre de chaque page (Bons cadeaux, Périodes fermées, Forfaits, Lieux, Réservations, Nouveau set), qui étaient restés à la taille de texte par défaut alors que la page Sets servait de référence.
- **Boutons « Enregistrer » rouges, actifs seulement si quelque chose a changé**. Nouveau hook `lib/use-form-dirty.ts` : compare un instantané du formulaire à son état initial à chaque saisie. Appliqué aux quatre formulaires d'édition concernés (exemplaire, forfait, fiche d'un set existant, profil client) : le bouton reste gris et non cliquable tant que rien n'a changé, redevient rouge dès la première modification, et regrise après un enregistrement réussi. `SubmitButton` affiche désormais un vrai gris neutre dans ce cas, pas une version juste assombrie de sa couleur. Les formulaires de création restent inchangés (toujours actifs).
- **Fichier de référence du design système** (`docs/DESIGN-SYSTEM.md`, nouveau). Couleurs et leur rôle, règles apprises par itération (rouge trop fort pour une identité, `sky` invisible en surbrillance, teintes diluées plutôt que couleurs pleines), boutons et leurs variantes, composants de formulaire, navigation, éléments flottants, conventions responsive. À tenir à jour à chaque décision de design, comme ce journal pour le reste.
- **Plusieurs créneaux horaires par lieu de remise** (migration 0012). Un lieu avait au plus une seule plage horaire (`open_from`/`open_until`) ; remplacée par une liste de créneaux (`slots`, jsonb `{ from, until }[]`), pour permettre par exemple 09:00–12:00 et 17:00–19:00 sur un même lieu. Ajoutés et retirés un par un dans le formulaire (`app/admin/lieux/forms.tsx`), transmis en JSON via un champ caché plutôt qu'en plusieurs champs nommés (validé et reparsé côté serveur, `lib/validation.ts`). Migration écrite à la main (le générateur de `drizzle-kit` demande une confirmation interactive pour ce genre de renommage de colonnes, impossible dans cet environnement) : elle recrée d'abord les créneaux existants à partir des anciennes colonnes avant de les supprimer, aucune donnée perdue. `isWithinOpening()` (`lib/bookings.ts`) et le tunnel de réservation (`booking-form.tsx`) vérifient désormais l'heure demandée contre l'ensemble des créneaux, plus une seule plage.
- **Fenêtre des lieux de remise, alignée sur le reste de l'admin**. Bouton « Ajouter le lieu » (bleu) et « Enregistrer » (vert) repassés en rouge, comme les autres formulaires de création et d'édition ; « Enregistrer » gagne le même comportement gris-tant-que-rien-n'a-changé que les autres fiches (exemplaire, forfait, set, profil). Bouton de suppression repassé au style simple (texte souligné) au lieu d'un bouton plein, pour matcher les lieux de fermetures et forfaits.
- **Sous-titres de l'espace de gestion, longueur alignée sur la page Sets**. Les sous-titres de Bons cadeaux, Périodes fermées, Forfaits, Lieux et Réservations faisaient deux à trois phrases ; ramenés à une seule phrase courte, la page Sets servant de référence de longueur. Le détail perdu (ex. « un set déjà loué peut rester chez le client », l'exemple de blocage via « En réparation ») n'est pas repris ailleurs pour l'instant.
- **Boutons « Enregistrer » repassés au vert**. Rouge jugé trop proche visuellement d'une suppression pour une simple sauvegarde ; passés en `leaf` (vert) partout où le comportement gris-tant-que-rien-n'a-changé s'applique (exemplaire, forfait, lieu, set existant, profil client). Rouge conservé pour les créations et les suppressions.
- **Rouge réservé aux actions destructrices, revu une dernière fois**. Le bouton « + Ajouter un créneau » (lieux de remise) réutilisait par erreur le style texte rouge souligné du bouton de suppression (`ConfirmButton`) : remplacé par un petit bouton jaune plein. En creusant le même problème ailleurs : le lien « Modifier » du tableau des sets était rouge alors que ce n'est ni une création ni une suppression (passé en bleu, `text-sea`) ; et surtout, tous les boutons de création (« Ajouter un set », « Ajouter le lieu », « Créer le forfait », « Ajouter l'exemplaire », « Générer » des bons cadeaux, « Ajouter » une fermeture, « Set remis », « Set rendu », « Accepter ces dates ») sont repassés du rouge au vert : la couleur ne distingue plus « action principale » de « action secondaire », mais uniquement « ça détruit ou ça refuse quelque chose » (rouge) de « ça construit ou fait avancer quelque chose » (vert). Après ce changement, quasiment plus rien n'est rouge dans l'espace de gestion à part les suppressions, le refus d'une demande et le blocage d'un client.

- **Suppression d'un lieu de remise, limitée aux réservations en cours**. Le blocage comptait n'importe quelle réservation ayant jamais utilisé ce lieu, y compris rendues ou annulées depuis longtemps : un lieu ayant servi une fois restait bloqué à vie. Ne compte plus que les réservations aux statuts actifs (`RESERVING_STATUSES` : en attente, acceptée, en location…) ; un lieu qui n'a plus que de l'historique redevient supprimable (`app/admin/lieux/actions.ts`).

- **Empêcher une date de fin avant la date de début, partout où c'est possible**. Les périodes de fermeture (« Du » / « Au ») et les créneaux horaires d'un lieu (« de » / « à ») laissaient saisir une fin antérieure au début (la validation serveur existait déjà et refusait, mais rien ne l'empêchait à la saisie). Le champ de fin prend maintenant `min` sur le champ de début, et recule automatiquement avec lui si besoin. Bouton « Ajouter » d'une fermeture gris tant que les deux dates ne sont pas renseignées ; bouton « Enregistrer » d'une fermeture existante passé au même comportement gris-tant-que-rien-n'a-changé que le reste (oublié lors du passage au vert plus tôt dans la session).

- **Bouton « Modifier » du tableau des sets, vraiment compact**. Le rétrécir (`text-xs py-1.5 px-3`) n'avait aucun effet visible : `.btn` (`app/globals.css`) vivait hors `@layer components`, donc sa taille par défaut (`padding: 1rem 1.75rem`, `font-size: 1.0625rem`) gagnait toujours sur les utilitaires Tailwind, quel que soit l'ordre des classes — une règle CSS non « layered » a toujours priorité sur les utilitaires, même écrits après elle. `.btn` et ses variantes (`.btn-brick`, `.btn-sun`…) déplacées dans `@layer components`, comme l'était déjà `.brick-card` pour la même raison. Tous les boutons compacts du site (« Modifier les dates », les boutons d'un `ConfirmButton`, « + Ajouter un créneau »…) en profitent au passage : ils étaient probablement tous rendus plus grands que prévu sans que ça saute aux yeux, faute d'un cas aussi visiblement à l'étroit qu'une colonne de tableau.

- **Filtres des bons cadeaux, instantanés comme ceux des sets**. Le tri (État, Origine, Recherche) passait par un rechargement de page (formulaire `GET` + bouton « Filtrer », filtrage en SQL). Remplacé par un filtrage côté client sans rechargement, sur le modèle de `components/admin/sets-table.tsx` : zone de filtre dans la même carte bleutée, bouton « Filtrer » supprimé, chaque sélection ou frappe met à jour la liste tout de suite (`app/admin/bons-cadeaux/vouchers-list.tsx`, nouveau). Au passage, extrait `giftVoucherDisplayStatus` (déduit « expiré » de la date) de `lib/gift-vouchers.ts` vers `lib/gift-vouchers-core.ts`, sans dépendance serveur (`lib/gift-vouchers.ts` importe `node:crypto` pour générer les codes, injouable dans un composant client) — même geste que `lib/availability-core.ts` plus tôt dans la session, pour la même raison.

### 18 septembre 2026

- **La facturation devient l'émission de notes** (spécification, module 8). Décision de la cliente : le site **n'établit pas de factures**, ce n'est pas un logiciel de facturation. Il automatise les **notes** — le document de vente destiné aux particuliers, aux obligations allégées. Mentions retenues : date de rédaction, nom et adresse de l'entreprise, nom du client sauf opposition de sa part, date et lieu d'exécution de la prestation, décompte détaillé en quantité et en prix, somme totale à payer. La note n'est disponible **qu'après le paiement**. Tombent avec la facture : la numérotation repartant de zéro et toute mention de facturation professionnelle. Quatre questions ouvertes notées en §3 (référence, périmètre du décompte, opposition au nom, périodes d'export).
- **Exports du back-office précisés** (module 11). L'export des ventes se déclenche **depuis l'interface admin**, sur une période donnée, et contient le **CA total de la période** plus **une ligne par client** avec la date de paiement et le set loué — il remplace l'« export simple par année civile ». S'y ajoute l'**export des notes émises par lot sur une période, dans un zip**. L'export comptable complet reste en V2.
- **Notes : numérotation, périmètre et opposition tranchés** (même jour). Numéro **séquentiel `aaaa-mm-##`** (année, mois, numéro), attribué à l'émission donc au paiement — une demande refusée ou non payée ne consomme pas de numéro ; la séquence repart à `01` à chaque mois, elle est mensuelle et non annuelle. **Périmètre** : la note étant établie au paiement, elle ne porte que la location et le **forfait démontage de 20 € si le client l'a sélectionné à la réservation** — ce qui en fait une option du tunnel (module 5) et non plus seulement une pénalité constatée au retour (module 9). Les montants inconnus au paiement — **indemnité de retard**, forfait démontage quand le set revient monté sans avoir été sélectionné, retenues du barème — font l'objet d'une **note complémentaire**, qui ne facture que ces montants mais **rappelle la note initiale** (numéro, date, références du set, période de location) pour rattacher le retard à sa location — d'où un lien en base de la note complémentaire vers la note initiale. **Opposition au nom** : réglage de l'espace client (module 3) ; le nom reste sur le récapitulatif des ventes, interne, pour le recoupement comptable.
- **Contenu du décompte de la note initiale** (même jour). Le décompte identifie la prestation par les **références du set** (titre et numéro(s) de boîte, multiples possibles) et la **période de location**, pas seulement par un prix. La note **renvoie au contrat de location** (module 7) pour les conditions particulières, par la référence de réservation (`bookings.reference`) que portent déjà les deux documents.
- **Solde de la restitution par deux boutons** (module 9). L'action « Set rendu » se dédouble : **Restitution conforme**, qui clôt sans rien émettre, ou **Établir une note complémentaire**, qui ouvre la saisie des montants dus (retard, démontage, barème). L'un des deux est obligatoire — c'est ce geste qui clôt l'état des lieux.
- **Périodes d'export** (module 11) : champs **début** et **fin** libres dans l'admin, plus un **bouton « année civile » avec sélecteur d'année** qui remplit les deux champs. Vaut pour l'export des ventes comme pour l'export des notes en zip.
- Aucun code touché : les modules 8 et 11 (exports) ne sont pas commencés. Doc mise à jour : `specification-fonctionnelle.md` (modules 3, 5, 7, 8, 9, 11 et les deux récapitulatifs), `FONCTIONNEMENT.md` (documents émis, conservation), `AVANCEMENT.md`.

### 17 septembre 2026

- **Protection des routes sortie du proxy (dépréciation Clerk)**. `next dev` signalait la dépréciation de `createRouteMatcher`. La protection par motif d'URL est remplacée par un contrôle dans chaque ressource : `requireRole("admin")` en tête des dix pages de `/admin` (les Server Actions l'avaient déjà), `auth.protect()` sur `/compte` et `/compte/profil`. `proxy.ts` ne garde que `clerkMiddleware()`, qui reste indispensable pour le contexte d'authentification. Le point n'était pas cosmétique : un layout Next n'est pas une barrière, le routeur pouvant ne demander que le segment d'une page sans réexécuter le layout parent — retirer le motif sans garder chaque page aurait ouvert les lectures admin (réservations, clients, notes internes) à une requête RSC forgée. Vérifié : requête RSC anonyme sur `/admin/reservations` → 307, zéro octet de données. `requireRole()` renvoie toujours vers `/admin` en dur, d'où `auth.protect()` pour `/compte`, qui gère le retour tout seul.

- **Revue de sécurité, deux correctifs**. Les notes internes des gérants (`customers.adminNote`, `customers.blockedReason`, `bookings.adminNote`, `bookings.returnNote`) partaient dans la charge RSC de `/compte`, `/compte/profil` et du tunnel : les lignes entières étaient passées en prop à des composants client. Projection explicite des colonnes (`customerPublicColumns`, `bookingCustomerColumns` dans `lib/bookings.ts`), type `CustomerBooking` sur la carte de réservation. `safeReturnPath()` acceptait `/\hôte`, lu comme une origine externe par le navigateur : redirection ouverte fermée.

- **Calendrier de disponibilités sur la fiche set**. Cadre « Disponibilités » avec un mois à la fois, navigation sur six mois, trois états (disponible, déjà loué, fermé) et jours passés grisés. Calcul pur jour par jour (`computeDayAvailability()`), réutilisant la recherche d'exemplaire libre du tunnel : locations prévues, demandes en attente, battement et périodes fermées sont exclus.

### 16 septembre 2026

- **Base et fiche set** (`a189d0a`). Table `sets` alignée sur la spécification, migration 0003, battement global à 4 jours. Formulaire admin en cinq blocs, 10 photos max. Catalogue public avec statut du jour et fiche set. Correction des sous-requêtes de la liste admin (la photo principale n'apparaissait jamais) et de l'avertissement SSL de `pg` que Next affichait comme une erreur.
- **Lieux de remise et périodes fermées** (`b7c35cc`). Deux écrans admin, cinq lieux confirmés dans le seed.
- **Tunnel de réservation jusqu'à la demande en attente** (`6b99e7f`). Statuts `pending_review` et `date_proposed`, migration 0004 (type recréé, l'ajout de valeur d'enum n'étant pas utilisable dans la même transaction) et 0005 (durée minimale à 1 jour, plus de maximum). Tunnel client, espace `/compte`, écran Réservations avec accepter / refuser / proposer d'autres dates, blocage du client. Tests : recherche d'exemplaire libre (12 cas purs), création de demandes sur la base (battement, prolongation, fermetures).
- **Import des 28 sets de la cliente**. Script `scripts/import-sets.ts` (`pnpm db:import-sets 00/Sets_LEGO.csv`), parseur CSV maison, fusion des lignes de suite (Ninjago = 71720 + 70613), valeurs normalisées, un exemplaire par set, publiés sans photo. Relance sans doublon. Catalogue : 29 sets, tous disponibles.
- **Filtre par gamme sur le catalogue**. Pastilles avec compteur, paramètre d'URL `?gamme=`, rendu côté serveur.
- **Comptes** : inscription testée avec succès sur l'instance Clerk de développement (e-mail + code de vérification). Plan de passage en production noté en §4.
- **Gestion du compte** : « Gérer le compte » du bouton utilisateur envoyait vers la page des locations. Nouvelle page `/compte/profil` avec le composant Clerk `UserProfile`, lien depuis `/compte`.
- **Coordonnées et historique dans la gestion du compte**. Onglet Coordonnées (identité, adresse, téléphone pour contrat et facture, lieu de remise préféré pré-sélectionné dans le tunnel), onglet Historique des réservations. Migration 0006 (`customers.preferred_pickup_point_id`). Correctif : les onglets n'apparaissaient pas en ligne, Clerk exige qu'ils soient déclarés dans un composant client.
- **Remise et retour dans l'admin**. Actions « Set remis » (dès l'acceptation, le loyer pouvant être réglé par TPE) et « Set rendu » avec date et état des lieux libre (migration 0007, `bookings.return_note`). Liste admin en cinq groupes, retard calculé à partir de J+1 et affiché en rouge, alerte sur le tableau de bord. Un set dehors non rendu reste indisponible au catalogue et pour les demandes. Suivi côté client sur chaque carte. Tests : retard et fin effective (unitaires), set en retard sur la base (indisponible, puis libre une fois rendu).
- **Coordonnées obligatoires avant de réserver**. L'inscription mène à l'onglet Coordonnées ; la page de réservation renvoie vers cet onglet tant que la fiche est incomplète, puis ramène à la réservation. Cadre Clerk sans défilement interne, téléphone au format 06 12 34 56 78.
- **Liste des réservations refondue, heure de remise**. Cartes lisibles (période en dates courtes, lieu et heure de remise, téléphone) avec les boutons Accepter, Modifier, Refuser sur les demandes à traiter. Heure de remise demandée dans le tunnel, modifiable par les gérants (migration 0008, `bookings.pickup_time`). Bandeau jaune du tableau de bord et fonds de cartes enfin appliqués (`.brick-card` passé dans `@layer components`).
- **Plage horaire par lieu de remise** (migration 0009). Les gérants fixent, lieu par lieu, les heures entre lesquelles le client peut demander la remise ; le tunnel borne le champ heure et le serveur revérifie.
- **Décision simplifiée sur une demande**. Plus de proposition d'autres dates : les gérants modifient seulement le lieu et l'heure de remise, puis acceptent (vert) ou refusent (rouge, bouton). Lieux : boutons colorés (ajouter bleu, enregistrer vert, supprimer rouge), interrupteur et suppression sur une ligne.
- **Bons cadeaux, première version admin** (`e11db65`, fusionné avec les 46 commits ci-dessus dans `d3ed70a`). Table `gift_vouchers` (migration 0010, appliquée après `vercel env pull` sur le nouveau compte Vercel payant) et écran `/admin/bons-cadeaux` : génération à l'unité ou en lot (même montant, étiquette de lot facultative, note interne), liste avec filtres (état, origine, recherche par code ou lot), marquer utilisé, annuler. Conflit de fusion réel sur `lib/format.ts` : `formatDate` existait déjà côté `main` pour une date ISO jour seul (réservations) ; renommé `formatDateTime` côté bons cadeaux, qui formate un timestamp complet. Pas encore branché sur le tunnel de réservation ni sur un achat en ligne (module 4, paiement).

### Avant le 16 septembre 2026

- Site vitrine repris de l'ancien site : accueil, avis, presse, Vinted, contact, mentions légales, CGU, charte graphique.
- Socle : Next.js 16, Neon + Drizzle, Clerk avec rôles, Vercel Blob, seed, écrans admin Forfaits et Sets (première version).
